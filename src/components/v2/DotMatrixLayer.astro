---
---

<div class="dot-layer" aria-hidden="true"></div>

<style>
  .dot-layer {
    position: absolute;
    inset: 0;
    pointer-events: none;
    z-index: 0;
    background-image: radial-gradient(
      circle,
      rgba(242, 237, 228, 0.09) 1px,
      transparent 1px
    );
    background-size: 24px 24px;
    /* Mask: fade near top (nav overlap) and edges */
    -webkit-mask-image:
      linear-gradient(
        to bottom,
        transparent 0%,
        rgba(0,0,0,0.5) 10%,
        rgba(0,0,0,1) 22%,
        rgba(0,0,0,1) 78%,
        rgba(0,0,0,0.4) 92%,
        transparent 100%
      );
    mask-image:
      linear-gradient(
        to bottom,
        transparent 0%,
        rgba(0,0,0,0.5) 10%,
        rgba(0,0,0,1) 22%,
        rgba(0,0,0,1) 78%,
        rgba(0,0,0,0.4) 92%,
        transparent 100%
      );
    opacity: 0;
    animation: dotLayerReveal 3s ease-out 0.1s forwards;
  }

  @keyframes dotLayerReveal {
    to { opacity: 1; }
  }
</style>
